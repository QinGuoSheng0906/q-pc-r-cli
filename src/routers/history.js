// 项目history.js 文件，仅供参考
import { HashHistoryBuildOptions } from 'history'
// const env = process.env.NODE_ENV
let options = {}

// if (env === 'production') {
//     options.basename = '/'
// }

export default HashHistoryBuildOptions(options)